# Next.js Environment Configuration Template
# Create a localized `.env.local` for development secrets.
# NEVER commit the final .env.local map directly into version control.

# Example API URL
# NEXT_PUBLIC_API_URL="http://localhost:8080/v1"
